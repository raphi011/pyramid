apiVersion: v1
kind: Service
metadata:
  name: {{ include "pyramid.fullname" . }}
  labels:
    {{- include "pyramid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pyramid.selectorLabels" . | nindent 4 }}
